#????????? ????? ? ??????? ? ??????????? ??????? ????? ?????? ?? ???????? ?? ????? ?? ????? ? 
mydata <- read.csv("C:/Users/user/PycharmProjects/R_project/utilities.csv")
plot(Fuel_cost_per_month ~ Number_of_people, data = mydata)

# ????????????? ???????
z <- mydata[,-c(1,1)]
means <- apply(z,2,mean)
sds <- apply(z,2,sd)
nor <- scale(z,center=means,scale=sds)
 
# ??????????? ????????? ?????????
distance = dist(nor)
 
# ?????????? ?????  
mydata.hclust = hclust(distance)
plot(mydata.hclust)

#??? ????????? ??????
kc<-kmeans(z,3)
plot(Number_of_people ~ Profit, data = mydata, col=kc$cluster)
plot(Fuel_cost_per_month ~ Profit, data = mydata, col=kc$cluster)
plot(Fixed_charge ~ Salary_per_hour, data = mydata, col=kc$cluster)
plot(Average_working_hours_per_week ~ Fuel_cost_per_month, data = mydata, col=kc$cluster)
